import styled from 'styled-components';
import { palette } from 'styled-theme';


const errorStyle = {
  fieldsetStyle: {
    marginTop: -10,
    paddingLeft: 5,
  },
  
  labelStyle: {
    fontSize: 13,
    color: "red"
  },
  
  inputStyle: {
    borderRadius: 4,
    borderColor: "red",
  }
}

export {
    errorStyle
};